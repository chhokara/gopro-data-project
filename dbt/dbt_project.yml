name: gopro_data
version: "1.0.0"
config-version: 2

profile: gopro_data

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gopro_data:
    staging:
      +materialized: view
    marts:
      +materialized: table
